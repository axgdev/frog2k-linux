diff --git a/drivers/mmc/host/hc15_mmc.c b/drivers/mmc/host/hc15_mmc.c
index b4d8a86..a7d9b0e 100644
--- a/drivers/mmc/host/hc15_mmc.c
+++ b/drivers/mmc/host/hc15_mmc.c
@@ -59,7 +59,7 @@
 #define HC15_DATA_VARIANTS	1
 #define HC15_READ_VARIANTS	1
 #define HC15_TRACE_VERBOSE	0
-#define HC15_PROBE_TAG		0x0223
+#define HC15_PROBE_TAG		0x0224
 
 struct hc15_mmc {
 	struct mmc_host *mmc;
@@ -86,7 +86,7 @@ static void hc15_mark(const char *name, u32 value)
 	    strcmp(name, "hc15-probe") &&
 	    strcmp(name, "hc15-add-host") &&
 	    !strstr(name, "fail") &&
-	    !strstr(name, "error") &&
+	    !(strstr(name, "error") && value) &&
 	    !strstr(name, "timeout") &&
 	    !strstr(name, "recover"))
 		return;
